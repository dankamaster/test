{
  "name": "coi-seo-pwa-test-assignment",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone SEO/PWA article page with automated QA checks for HTML, accessibility, schema, links, privacy UI and Lighthouse.",
  "scripts": {
    "test": "npm run test:repo && npm run test:html && npm run test:a11y && npm run test:schema && npm run test:links && npm run test:privacy",
    "test:repo": "node scripts/check-repo-structure.mjs",
    "test:html": "node scripts/check-html.mjs",
    "test:a11y": "node scripts/check-a11y.mjs",
    "test:schema": "node scripts/check-schema.mjs",
    "test:links": "node scripts/check-links.mjs",
    "test:privacy": "node scripts/check-privacy-ui.mjs",
    "serve": "vite --host 127.0.0.1 --port 4173",
    "lhci": "lhci autorun"
  },
  "devDependencies": {
    "@lhci/cli": "^0.14.0",
    "cheerio": "^1.0.0",
    "vite": "^5.4.0"
  }
}
